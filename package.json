{
  "name": "zazawoods-3d-configurator",
  "version": "1.0.0",
  "description": "Zaza Woods 3D furniture configurator — self-contained static site",
  "private": true,
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
